# COA Cleanup GL 1121–1123 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mark asset-side savings accounts GL 1121, 1122, 1123 as `is_postable => false` in `SaccoCoaSeeder` so they are excluded from manual posting dropdowns.

**Architecture:** Single seeder edit — change `is_postable => true` to `is_postable => false` on three rows. No migrations, no service changes.

**Tech Stack:** Laravel 12, PHP 8.2, `SaccoCoaSeeder`, `coa_template_accounts` table on master DB.

---

## File Map

| File | Action |
|------|--------|
| `database/seeders/SaccoCoaSeeder.php` | Set `is_postable => false` on rows 1121, 1122, 1123 |

---

## Task 1 — Mark 1121, 1122, 1123 as non-postable

**Files:**
- Modify: `database/seeders/SaccoCoaSeeder.php`

- [ ] **Step 1: Update the three rows in `getSaccoUgandaAccounts()`**

Find the current rows (around lines 77–79):

```php
['gl_code' => '1121', 'name' => 'Mandatory Savings – Members', 'account_type' => 'ASSET', 'account_subtype' => 'Member Savings', 'normal_balance' => 'DR', 'level' => 4, 'is_control' => false, 'is_postable' => true],
['gl_code' => '1122', 'name' => 'Voluntary Savings – Members', 'account_type' => 'ASSET', 'account_subtype' => 'Member Savings', 'normal_balance' => 'DR', 'level' => 4, 'is_control' => false, 'is_postable' => true],
['gl_code' => '1123', 'name' => 'Fixed Deposit – Members', 'account_type' => 'ASSET', 'account_subtype' => 'Member Savings', 'normal_balance' => 'DR', 'level' => 4, 'is_control' => false, 'is_postable' => true],
```

Replace with:

```php
['gl_code' => '1121', 'name' => 'Mandatory Savings – Members', 'account_type' => 'ASSET', 'account_subtype' => 'Member Savings', 'normal_balance' => 'DR', 'level' => 4, 'is_control' => false, 'is_postable' => false],
['gl_code' => '1122', 'name' => 'Voluntary Savings – Members', 'account_type' => 'ASSET', 'account_subtype' => 'Member Savings', 'normal_balance' => 'DR', 'level' => 4, 'is_control' => false, 'is_postable' => false],
['gl_code' => '1123', 'name' => 'Fixed Deposit – Members', 'account_type' => 'ASSET', 'account_subtype' => 'Member Savings', 'normal_balance' => 'DR', 'level' => 4, 'is_control' => false, 'is_postable' => false],
```

Only `is_postable` changes — everything else stays identical.

- [ ] **Step 2: Lint**

```bash
composer test:lint
```

Expected: `SaccoCoaSeeder.php` not in failure list.

- [ ] **Step 3: Commit**

```bash
git add database/seeders/SaccoCoaSeeder.php
git commit -m "fix: mark GL 1121/1122/1123 as non-postable — correct savings accounts are 2111/2112/2113 (#13)"
```
